# prismauth

somewhat rudimentary mod to re-auth or switch accounts using your prismlauncher
accounts.json. it also works as a library, the menu button is only added if
loaded as a standalone mod but `PrismAuth.loadAccounts()` can be called from
elsewhere to get account details.
